/**
 * join()
 * joins array elements tother into a string
 * allows user to sepcify the sperator 
 */

const fruits = ["Banana", "Orange", "Apple", "Mango"];
myFruits = fruits.join(" / ");
console.log(myFruits);