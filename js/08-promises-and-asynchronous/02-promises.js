/************************PROMISES*********************************************/

//creating a new promise
// a promise should always have an executor function, therefore new Promise(); alone will return an error
let promise = new Promise(functiion(){
    
}); 
